{
  "name": "grandomize",
  "version": "0.1.2",
  "type": "commonjs",
  "description": "Randomizer module.",
  "main": "dist/index.js",
  "files": [
    "dist",
    "assets"
  ],
  "scripts": {
    "test": "jest --verbose",
    "dist": "rm -rf dist && mkdir dist && cp src/index.js dist && cp src/names.js dist && cp src/random.js dist && npm pack --dry-run",
    "clean": "rm -rf node_modules && rm package-lock.json"
  },
  "author": "Stefan Gerdin",
  "license": "ISC",
  "devDependencies": {
    "jest": "^27.0.4"
  }
}
