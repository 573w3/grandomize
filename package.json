{
  "name": "grandomize",
  "version": "0.1.0",
  "type": "commonjs",
  "description": "Randomizer module.",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "jest --verbose",
    "dist": "rm -rf dist && mkdir dist && cp -r assets dist && cp src/index.js dist && cp src/names.js dist && cp src/random.js dist",
    "clean": "rm -rf node_modules && rm package-lock.json"
  },
  "author": "Stefan Gerdin",
  "license": "ISC",
  "devDependencies": {
    "jest": "^27.0.4"
  }
}
