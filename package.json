{
  "name": "grandomize",
  "version": "0.1.5",
  "type": "commonjs",
  "description": "Randomizer module.",
  "main": "dist/index.js",
  "files": [
    "dist",
    "assets"
  ],
  "scripts": {
    "test": "npm run test:jest && npm run test:visual",
    "test:jest": "jest --verbose",
    "test:visual": "node visual.test.js",
    "dist": "rm -rf dist && mkdir dist && cp src/index.js dist && cp src/names.js dist && cp src/random.js dist && cp src/addresses.js dist && cp src/assetLoader.js dist && cp src/languages.js dist && cp src/utils.js dist && npm pack --dry-run",
    "clean": "rm -rf node_modules && rm package-lock.json"
  },
  "author": "Stefan Gerdin",
  "license": "ISC",
  "devDependencies": {
    "jest": "^27.0.4"
  }
}
